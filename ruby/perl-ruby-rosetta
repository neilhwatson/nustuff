Quotes

qx{ /usr/bin/cmd }
%x{ /usr/bin/cmd }

qw{ one two }
%w{ one two }

String manipulation

mystring = join( ", ", @my_array )
mystring = myarray.join( ", " )

mystring = "str" . "str1"
mystring = "str" + "str1"

Debugging

Dumper( \@my_array )
pp my_array

Modules

use MyMod
require 'my_mod'

Documentation

POD vs Rdoc ?
